namespace MBus
{
    /// <summary>
    /// This acts as the main common interface for all sent messages.
    /// All messages being dispatched should be implementing that interface.
    /// </summary>
    public interface IMBusMessage
    {
    }
}